(function(){
  'use strict';
  const num=value=>Number(value)||0;
  const text=value=>String(value??'').trim();
  const safeFile=value=>text(value).replace(/[<>:"/\\|?*\x00-\x1f]/g,'_').replace(/[. ]+$/g,'');
  function companyView(){return {name:'酷舍企業有限公司',englishName:'Cool Sir Limited Company',taxId:'82980636',address:'台中市豐原區瑞興路 62 號',line:'eing0519'}}
  function publicLines(record,type){const mode=type==='quotation'?text(record.pricingMode):'',lines=(Array.isArray(record.lines)?record.lines:[]).map(line=>{const pricingType=mode==='mixed'?(line.pricingType==='lump_sum'?'lump_sum':'actual'):mode,rawQty=line.qty??line.quantity,hasQty=rawQty!==null&&rawQty!==undefined&&String(rawQty).trim()!==''&&num(rawQty)>0,amount=pricingType==='lump_sum'?num(line.lumpSumAmount??line.amount):hasQty?num(line.amount??num(rawQty)*num(line.price??line.unitPrice)):0;return {house:text(line.house||line.unitName||line.building),item:text(line.item||line.name||line.description),unit:text(line.unit),qty:hasQty?num(rawQty):null,price:pricingType==='lump_sum'?null:num(line.price??line.unitPrice),amount,pricingType}}).filter(line=>line.item||line.amount||line.qty);if(type==='quotation'&&mode==='lump_sum')lines.push({house:'',item:'合約／報價總價',unit:'式',qty:null,price:null,amount:num(record.lumpSumTotal??record.amount),pricingType:'lump_sum'});return lines}
  function externalView(record,settings,type){
    const lines=publicLines(record,type),sales=num(record.amount)||lines.reduce((sum,line)=>sum+line.amount,0),tax=num(record.tax),total=num(record.grossTotal)||num(record.total)||sales+tax;
    const legacyNote=text(record.note),publicNote=text(record.publicNote||(type==='quotation'||!/每日業績|施工紀錄.*彙總|確認後儲存|內部|ERP/i.test(legacyNote)?legacyNote:''));
    const pricing=text(record.pricingMode),pricingNote=type==='quotation'&&pricing==='actual'&&!publicNote.includes('實做實算')?'實做實算':'';
    return {schema:'kushe-external-document-v1',documentType:type==='quotation'?'quotation':'billing',title:type==='quotation'?'報價單':'請款單',number:text(record.number||record.externalNo),customer:text(record.customerName||record.customerLabel),project:text(record.projectName||record.projectLabel),date:text(record.date),taxMode:text(record.taxMode),pricingMode:pricing,invoiceStatus:text(record.invoiceStatus),lines,salesAmount:sales,taxAmount:tax,totalAmount:total,retentionAmount:type==='billing'?num(record.retentionAmount??record.retention):0,receivableAmount:type==='billing'?num(record.total):total,publicNote:[pricingNote,publicNote].filter(Boolean).join('\n'),company:companyView(settings)};
  }
  function openView(view,autoPrint){const project=view.project||view.customer||'',month=view.date.slice(0,7),prefix=view.documentType==='quotation'?'酷舍_報價單':'酷舍_請款單',fileName=safeFile([prefix,project,month].filter(Boolean).join('_'))+'.pdf',payload=encodeURIComponent(btoa(unescape(encodeURIComponent(JSON.stringify({...view,fileName}))))),url=new URL('billing-print.html?v=20260814-effectiveactual1&print='+(autoPrint?'1':'0'),location.href);url.hash=payload;location.assign(url.href)}
  function openRecord(record,type,autoPrint){if(!record)return;openView(externalView(record,window.KuSheERPStore.getState().settings,type),autoPrint)}
  window.KusheBillingPrint={export(row){openRecord(row,'billing',true)},print(row){openRecord(row,'billing',false)},buildExternalView(row){return externalView(row,window.KuSheERPStore.getState().settings,'billing')}};
  window.KusheQuotationPrint={export(row){openRecord(row,'quotation',true)},print(row){openRecord(row,'quotation',false)},buildExternalView(row){return externalView(row,window.KuSheERPStore.getState().settings,'quotation')}};
})();
