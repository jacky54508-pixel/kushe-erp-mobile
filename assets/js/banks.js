(function () {
  'use strict';
  const store=window.KuSheERPStore,$=(selector,root=document)=>root.querySelector(selector),esc=(value)=>String(value??'').replace(/[&<>"']/g,(char)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char])),money=(value)=>`$${new Intl.NumberFormat('zh-TW',{maximumFractionDigits:0}).format(Math.round(store.num(value)))}`;
  let active=false,ready=false;
  const bankName=(row)=>row.name||row.bank||row.account||'銀行帳戶';
  const balance=(row)=>store.num(row.openingBalance)+store.num(row.income)-store.num(row.expense);
  function render(){if(!active)return;const state=store.getState(),transactions=[...state.bankTransactions].sort((a,b)=>String(b.date||b.createdAt||'').localeCompare(String(a.date||a.createdAt||''))),total=state.banks.reduce((sum,row)=>sum+balance(row),0);$('#banksApp').innerHTML=`<section class="commissions-heading"><div><h1>銀行帳戶</h1><p>銀行餘額與一般應收收款交易使用同一份 ERP 資料。</p></div></section><section class="commission-kpis"><article><span>銀行總餘額</span><strong>${money(total)}</strong><small>${state.banks.length} 個帳戶</small></article>${state.banks.map((bank)=>`<article><span>${esc(bankName(bank))}</span><strong>${money(balance(bank))}</strong><small>期初 ${money(bank.openingBalance)}</small></article>`).join('')}</section><section class="commission-panel billing-list-panel"><header class="project-section-title"><div><h2>銀行交易明細</h2><p>一般收款以來源收款 ID 防止重複入帳。</p></div></header><div class="commission-table-wrap"><table class="commission-table"><thead><tr><th>日期</th><th>交易類型</th><th>來源</th><th>說明</th><th class="num">收入</th><th class="num">支出</th><th>帳戶</th></tr></thead><tbody>${transactions.map((row)=>{const bank=state.banks.find((item)=>item.id===(row.bankAccountId||row.bankId)),income=row.direction==='in'||/收入|入帳/.test(row.type||''),description=row.description||row.note||row.projectName||row.sourceNo||'—';return `<tr><td>${esc(row.date||'—')}</td><td>${esc(row.category||row.type||'—')}</td><td>${esc(row.projectName||row.customerName||row.vendorName||row.sourceNo||'—')}</td><td>${esc(description)}</td><td class="num">${income?money(row.amount):'—'}</td><td class="num">${income?'—':money(row.amount)}</td><td>${esc(bankName(bank||{}))}</td></tr>`}).join('')||'<tr><td colspan="7" class="billing-empty">尚無銀行交易。</td></tr>'}</tbody></table></div></section>`;window.KusheIcons?.render($('#banksApp'))}
  async function activate(){active=true;if(!ready){await store.load();ready=true}render()}
  function deactivate(){active=false}
  window.addEventListener('kushe:data-updated',render);
  window.KusheBanks={activate,deactivate,render};
}());
